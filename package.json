{
  "name": "react-node-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=14.0.0" 
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
